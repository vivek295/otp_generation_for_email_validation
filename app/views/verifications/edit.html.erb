<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<%= form_for(:user,url: verification_path,html: {method: "put"} ) do |f| %>
			<%= hidden_field_tag :email,@user.email %>
			<%= f.label :otp %>
			<%= f.text_field :otp ,value: "" %>
			<%= f.submit "Verify"%>
		<% end %>
	</div>
</div>